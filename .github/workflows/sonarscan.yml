name: Sonarscan
on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
jobs:
  sonarqube:
    name: Sonarscan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - name: Set up Lua
        uses: leafo/gh-actions-lua@v10
        with:
          luaVersion: "5.4"

      - name: Set up LuaRocks
        uses: leafo/gh-actions-luarocks@v4

      - name: Install test dependencies
        run: |
          luarocks install busted
          luarocks install luacov

      - name: Run tests with coverage
        run: busted --coverage tests/MapAchiever.test.lua

      - name: Generate coverage report for SonarQube
        run: |
          python3 tests/stats_to_sonar.py luacov.stats.out coverage/sonar-coverage.xml
          echo "--- sonar-coverage.xml ---"
          cat coverage/sonar-coverage.xml

      - name: Sonarscan
        uses: SonarSource/sonarqube-scan-action@1a6d90ebcb0e6a6b1d87e37ba693fe453195ae25
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
